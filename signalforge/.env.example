# Example only: server.py reads environment variables, not this file.
# Never commit a real key.
OPENAI_API_KEY=
# Choose an available Responses API model that supports web_search.
OPENAI_MODEL=
PORT=8787
